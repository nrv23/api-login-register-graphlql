type Mutation {

  #agregar usuarios

  add(user: UserInput!): Result!  # siempre user types Input para los mutation y así respetar los valores que sean o no obligatorios

  #actulizar usuarios


  update(user: UserInput!): Result!
}